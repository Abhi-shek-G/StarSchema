# StarSchema
A star schema data warehouse model built using PostgreSQL on a retail sales dataset. The project covers raw data ingestion, dimension and fact table creation, and analytical queries to support business reporting.
